# Get total size of a remote path

Get total size of a remote path

## Usage

``` r
rclone_size(path)
```

## Arguments

- path:

  Remote path.

## Value

A named list with `count` (number of objects) and `bytes` (total size in
bytes).
