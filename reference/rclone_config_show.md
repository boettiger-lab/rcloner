# Show rclone configuration

Show rclone configuration

## Usage

``` r
rclone_config_show(name = NULL)
```

## Arguments

- name:

  Optional remote name. If omitted, shows all remotes.

## Value

Character string with the configuration (invisibly). Also prints to the
console.
