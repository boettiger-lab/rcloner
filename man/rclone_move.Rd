% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rclone_copy.R
\name{rclone_move}
\alias{rclone_move}
\title{Move files between remotes or local paths}
\usage{
rclone_move(src, dest, progress = FALSE, ...)
}
\arguments{
\item{src}{Source path (local or remote).}

\item{dest}{Destination path (local or remote).}

\item{progress}{Logical; show transfer progress. Default \code{FALSE}.}

\item{...}{Additional rclone flags as character strings.}
}
\value{
Invisibly, the rclone result list.
}
\description{
Moves files from \code{src} to \code{dest}. Source files are deleted after a
successful transfer.
}
\examples{
\dontshow{if (rclone_available()) withAutoprint(\{ # examplesIf}
src <- tempfile()
dir.create(src)
writeLines("hello", file.path(src, "file.txt"))
dest <- tempfile()
rclone_move(src, dest)
\dontshow{\}) # examplesIf}
}
