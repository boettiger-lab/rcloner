% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rclone_ls.R
\name{rclone_ls}
\alias{rclone_ls}
\title{List objects in a remote path}
\usage{
rclone_ls(path, recursive = FALSE, dirs_only = FALSE, files_only = FALSE, ...)
}
\arguments{
\item{path}{Remote path in rclone syntax, e.g. \code{"myremote:bucket/prefix"}.
For local paths use the filesystem path directly.}

\item{recursive}{Logical; if \code{TRUE}, list all objects recursively. Default
\code{FALSE}.}

\item{dirs_only}{Logical; if \code{TRUE}, list only directories. Default \code{FALSE}.}

\item{files_only}{Logical; if \code{TRUE}, list only files (no directories).
Default \code{FALSE}.}

\item{...}{Additional flags passed to \verb{rclone lsjson} as strings (e.g.
\code{"--fast-list"}).}
}
\value{
A data frame with columns \code{Path}, \code{Name}, \code{Size}, \code{MimeType},
\code{ModTime}, \code{IsDir}, and optionally others returned by rclone.
}
\description{
Returns a data frame of objects at the given path, parsed from rclone's JSON
output. This is the primary listing function and maps to \verb{rclone lsjson}.
}
\examples{
\dontshow{if (rclone_available()) withAutoprint(\{ # examplesIf}
rclone_ls(tempdir())
rclone_ls(tempdir(), recursive = TRUE)
\dontrun{
rclone_ls("myremote:mybucket")
}
\dontshow{\}) # examplesIf}
}
