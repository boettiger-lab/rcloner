% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_rclone.R
\name{install_rclone}
\alias{install_rclone}
\title{Install the rclone binary}
\usage{
install_rclone(
  force = FALSE,
  dest = getOption("rcloner.dir", tools::R_user_dir("rcloner", "data"))
)
}
\arguments{
\item{force}{Logical; if \code{TRUE}, re-download even if rclone is already
installed. Default \code{FALSE}.}

\item{dest}{Directory in which to install rclone. Defaults to the value of
\code{options("rcloner.dir")}, falling back to \code{tools::R_user_dir("rcloner", "data")}.}
}
\value{
Invisibly, the path to the installed rclone binary.
}
\description{
Downloads and installs the rclone binary for the current platform.
The binary is stored in a user-writable directory and does not require
system-level installation.
}
\examples{
\dontrun{
install_rclone()
}
}
