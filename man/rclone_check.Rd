% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rclone_ops.R
\name{rclone_check}
\alias{rclone_check}
\title{Check whether two remotes are in sync}
\usage{
rclone_check(src, dest, ...)
}
\arguments{
\item{src}{Source path.}

\item{dest}{Destination path.}

\item{...}{Additional rclone flags.}
}
\value{
Invisibly, the rclone result list (check stderr for differences).
}
\description{
Compares the files in \code{src} and \code{dest} without transferring anything.
Differences are reported as messages.
}
\examples{
\dontshow{if (rclone_available()) withAutoprint(\{ # examplesIf}
src <- tempfile()
dir.create(src)
dest <- tempfile()
dir.create(dest)
rclone_check(src, dest)
\dontshow{\}) # examplesIf}
}
