% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rclone_config.R
\name{rclone_config_create}
\alias{rclone_config_create}
\title{Configure an rclone remote}
\usage{
rclone_config_create(name, type, ...)
}
\arguments{
\item{name}{Name for the remote (used as the prefix in paths, e.g.
\code{"myremote:bucket/key"}).}

\item{type}{Remote type, e.g. \code{"s3"}, \code{"gcs"}, \code{"azureblob"}, \code{"drive"}.
Run \code{rclone("help backends")} to see all supported types.}

\item{...}{Named parameters passed to the remote configuration. For S3
remotes this typically includes \code{provider}, \code{access_key_id},
\code{secret_access_key}, and optionally \code{endpoint} (for non-AWS S3).}
}
\value{
Invisibly, the result of the rclone command.
}
\description{
Creates or updates a named remote in the rclone config file. This is the
primary way to register cloud storage credentials with rclone.
}
\examples{
\dontrun{
# AWS S3
rclone_config_create("aws", type = "s3",
  provider = "AWS",
  access_key_id = Sys.getenv("AWS_ACCESS_KEY_ID"),
  secret_access_key = Sys.getenv("AWS_SECRET_ACCESS_KEY"),
  region = "us-east-1"
)

# MinIO / S3-compatible
rclone_config_create("minio", type = "s3",
  provider = "Minio",
  access_key_id = Sys.getenv("MINIO_ACCESS_KEY"),
  secret_access_key = Sys.getenv("MINIO_SECRET_KEY"),
  endpoint = "https://minio.example.com"
)
}
}
