% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rclone_copy.R
\name{rclone_copy}
\alias{rclone_copy}
\title{Copy files between remotes or local paths}
\usage{
rclone_copy(src, dest, progress = FALSE, ...)
}
\arguments{
\item{src}{Source path (local or remote).}

\item{dest}{Destination path (local or remote).}

\item{progress}{Logical; show transfer progress. Default \code{FALSE}.}

\item{...}{Additional rclone flags as character strings.}
}
\value{
Invisibly, the rclone result list.
}
\description{
Copies files from \code{src} to \code{dest}, skipping files that are identical.
Does not delete files at the destination.
}
\examples{
\dontshow{if (rclone_available()) withAutoprint(\{ # examplesIf}
src <- tempfile()
dir.create(src)
writeLines("hello", file.path(src, "file.txt"))
dest <- tempfile()
rclone_copy(src, dest)
list.files(dest)
\dontrun{
rclone_copy("myremote:bucket/file.csv", "/tmp/file.csv")
}
\dontshow{\}) # examplesIf}
}
